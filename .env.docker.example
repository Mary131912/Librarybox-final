# Variables de entorno para Docker - LibraryBox
# Copia este archivo a .env y configura tus valores

# MongoDB Atlas (recomendado para producción)
# Reemplaza con tu connection string de MongoDB Atlas
MONGODB_URI=mongodb+srv://tu_usuario:tu_password@cluster0.xxxxx.mongodb.net/auth-app?retryWrites=true&w=majority&appName=Cluster0

# JWT Secret - Genera uno único y seguro
# Puedes usar: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=tu_secreto_super_seguro_cambiar_en_produccion

# Puerto de la aplicación (dejar en 3000)
PORT=3000

# Entorno (production para Docker)
NODE_ENV=production
